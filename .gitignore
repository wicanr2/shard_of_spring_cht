# 原版素材:一律不進版控(見 CLAUDE.md §1「不做的事」)
# ⚠ 開頭的 / 是必要的 —— 不錨定的 `original/` 會匹配**任何深度**的同名目錄,
#   曾經把 engine/internal/original/ 整包擋掉,而 commit 仍然成功
#   (clone 下來才會發現編不起來)。
/original/
/game/
*.zip
*.rar

# IDA 產物
workplace/
*.i64
*.id0
*.id1
*.id2
*.nam
*.til
*.asm

# 反組譯／分析的中間產物
tools/scratch/
*.log
result.txt

# 截圖與錄影
*.png
*.jpg
*.jpeg
*.gif
*.mp4
!docs/images/*.png
!docs/images/**/*.png
!assets/**/*.png
# ⚠ 上面三行是 CLAUDE.md §8 的例外(專案負責人裁定 2026-08-15):
#    引擎畫面的截圖、以及 cmd/convert 轉出的資產,都可以進版控 ——
#    **即使那是原版美術**。理由與 translations/ 收錄原版英文文本相同。
# ⛔ 例外**不涵蓋原版檔案本身**:game/ 與 original/ 仍然一律 gitignore。
#    判準:轉換後的產物可以,原版磁片的內容不行。
# ⚠ 這讓 §10「repo 維持 private」變成整個專案最吃重的一條。

# 一般
.DS_Store
__pycache__/
*.pyc
.venv/

# Go 建置產物
build/
engine/vendor/
