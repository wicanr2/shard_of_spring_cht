# 《春之石》remake 協作規則

## 語言與必讀順序

- 面向使用者的文字與專案文件預設使用繁體中文；程式識別字、命令、路徑與產品名稱保留原文。
- 每個新工作階段依序讀取本檔、`CONTEXT.md`、`CLAUDE.md`，再讀與任務直接相關的規格或證據。
- `CONTEXT.md` 是目前狀態與研究索引的單一入口；剩餘工作以 `docs/spec/14-remake-worklist.md` 為準。歷史清單或舊對話不能單獨重新開啟已完成項目。
- 下手前先搜尋 `docs/`、`tools/`、程式碼與 `translations/glossary.md`，避免重做或採用已被推翻的舊結論。

## 專案定位與目前狀態

- 本專案是 SSI《Shard of Spring》MS-DOS 版的 Go + Ebitengine 乾淨重製與繁體中文化，定位為文化資產保存。
- RE 看板 A–L 已全部通過；引擎 M0–M8、正常玩家路徑與三平台 `v0.4.0` 已完成。綠色測試只證明 remake 內部自洽，不能自行升格為原版 parity。
- RE 深度止於會影響玩家行為的遊戲機制；不要深挖無關的 DOS／BIOS 實作。圖形格式只追到能正確轉換、顯示與驗證。
- 新發現若會改變玩家體驗或既有完成判定，才重新開啟窄範圍研究；不得把收尾變成無限逆向。

## 證據與規格閘門

- 工作流為 `IDA 證據 → docs/re/ → docs/formats/ 或 docs/spec/ → READY → 實作`。新機制或新格式沒有 READY 規格，不得猜測實作。
- 反組譯只使用既有 `ida-pro-9.4-idapython:py312-v1` 與 `tools/ida.sh`；IDA `.i64` 的交叉參照、函式邊界與資料流是主證據，攤平 `.asm` 只作搜尋線索。
- 每項研究結論使用「已確認／證據充分／假設／未知」，並保留輸入檔、SHA-256、IDA linear address、原始名稱／運算元與證據出處。不得用推測性改名覆蓋原始定位。
- 原版 DOS 行為的證據優先序是：真人實測、IDA、官方手冊／攻略；套用手冊前先確認平台版本。
- 推翻結論時改正正文，並在 `CONTEXT.md` 保留舊斷言、反證與訂正理由。

## 原版、美化主題與音訊

- 原版忠實呈現與重製增補必須分開；畫面或提示須讓玩家知道目前使用哪一種模式。現代美術不能冒充原版修復。
- 顯示基準維持 1024×768；既有原版美術採 4× 整數放大，文字層使用 TTF 原生解析度。若新增現代主題，不得破壞既有原版主題的驗收基準。
- 每個視覺主題必須是完整、預先驗證的資產群組，包含相符的 sprite、tileset、title、logo 與所需 palette／metadata；切換時以完整群組原子替換，不得顯示半載入或混搭狀態。
- 現代資產必須保留原版索引、尺寸契約、錨點、方向、碰撞語意、動畫格順序與秘密資訊邊界。視覺取捨先做可丟棄的並列原型，再由專案負責人決定。
- 現有配樂已有遊戲內 `F5` 切換：「原版」（只有通關曲與死亡曲）、「重製」（另加場景配樂）、「關閉」；設定存於獨立 `config.json`，不修改遊戲存檔 schema。
- 新配樂必須標示為重製增補，保留來源、作者／模型、日期、提示、種子、後製、授權條款與用途紀錄；發行前重新確認散布與商用權利。技術解碼或自動檢測不能取代人耳驗收。

## 實作與驗證

- 所有分析、批次搜尋、轉檔、建置、測試、抓圖、音訊與 GUI 工作一律在 Docker 內執行。優先沿用既有 `shard-go-build:6` 或專案工具 image；不得在主機執行 Python、Go、遊戲或分析腳本。
- 一次性容器使用 `docker run --rm --network none`，設定合理的 memory、CPU、PID 與外層逾時；可寫容器明確使用目前 UID/GID。`original/` 與 `game/` 唯讀，測試存檔寫入 `/tmp`。
- 寫入前檢查目標與輸出目錄 UID/GID；寫入後抽查擁有權。禁止對整個儲存庫遞迴 `chown`。
- 基準測試至少執行 `tools/go.sh test -count=1` 與 `tools/go.sh test -count=1 -tags eten`；不得省略 `-count=1`。視覺改動另需原版與現代主題的同狀態截圖、資產清冊／邊界測試、正常玩家路徑與主題來回切換測試。
- 有世界、地城、城鎮、一般戰鬥與最終戰等分支時，至少抽測各主要路徑；direct-entry、座標注入與 debug hook 不能取代正常玩家路徑。
- 每批 Docker 工作後檢查專案相關容器，停止並移除不再需要者；不得使用任何全域 prune，也不得刪除其他專案的 image、container 或 volume。

## 檔案、權利與 Git

- `original/`、`game/`、原始執行檔與磁片資料不得進 Git 或公開發行。private repo 可收錄既有裁定允許的 `translations/`、`docs/images/` 與轉換後 `assets/`，但公開套件仍不得附帶原版資產。
- 儲存庫必須維持 private；任何 release、連結分享或協作者變更前，先查驗實際 visibility。不得擅自轉為 public。
- 現代美術、logo 與配樂只有在權利鏈可追溯、且符合預定散布方式時才能納入正式資產或發行包。
- 保留使用者既有修改；`git add` 指名檔案。除非使用者明確要求，不自行 commit、push、發布或做破壞性 Git 操作。

## 共同決策

- 會改變美術風格、配樂方向、主題資料格式、save 相容性、授權、公開方式或玩家體驗的選擇，先查證事實，再一次提出一個最上層決策供使用者選擇。
- 每題交代已知事實、需要現在決定的原因、可行選項、建議、實際影響與單一問題。使用者確認前，只做不依賴決策的查證與可丟棄原型。
- 使用者確認重大方向後，先重述採用與排除選項，再更新 `CONTEXT.md`、ADR 或相符規格，最後才進正式實作。
