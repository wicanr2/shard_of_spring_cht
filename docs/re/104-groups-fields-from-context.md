# 104 —(部分作廢)`GROUPS.DAT` 再解四個欄位:從語境字串反推

> ⛔ **位移 25 與 31 判錯**([`107`](107-clock-is-four-levels.md)):
> 31 是**時鐘的第二級**、25 是**每次時鐘推進都遞減的倒數計時器**、
> **79 是迷宮座標 x**([`114`](114-maze-coordinates.md))。**四筆裡三筆是錯的。**
> 真相是計時碼就放在那些敘述附近。位移 45、79 用同一個方法得到,**待重驗**。

日期:2026-08-14
接續:[`103-flee-is-leaving-the-field.md`](103-flee-is-leaving-the-field.md)
子系統:**D. 角色/隊伍資料**

## 結論

`GROUPS.DAT` 出貨全空白,資料側沒有訊號([`101`](101-groups-record-status.md) §3)。
改用**「來源變數附近引用了哪些字串」**當標籤,再解出四個。

| 位移 | 來源 | 語意 | 語境字串 |
|---:|---|---|---|
| **25** | `ds:3658h` | **隊伍在哪一層地圖** | `MAZEMOVE` / `WRLDMOVE`(只有這兩個)|
| **31** | `ds:353Ah` | **當前所在的城鎮／設施** | `'Welcome to the'` + `"Fighter's Guild."` |
| **45** | `ds:365Ah` | **光源剩餘回合數** | `'You now have about'` + `' turns of light.'` |
| **79** | `ds:351Ch` | **當前迷宮編號** | `DE5EFF.BIN` / `DE51EFF.BIN`(迷宮事件檔,[`60`](60-g-closure-2.md))|

## 1. 為什麼語境字串可以當證據

一個變數被讀寫的**那幾行**附近,編譯器放的是**同一句敘述**用到的常數。
`'You now have about'` 與 `' turns of light.'` 是**同一句話的兩半** ——
中間夾的那個數字就是 `ds:365Ah`。

⚠ **這比直接讀指令弱一級**:我沒有逐條讀出「這個變數被印在那兩個字串之間」,
只看到它們在同一個小範圍內。**四筆都標證據充分,不標已確認。**

支持的旁證:

- 位移 25 的語境**只有 `MAZEMOVE` 與 `WRLDMOVE` 兩個字串**,沒有第三個 ——
  與「隊伍在世界地圖還是在迷宮」的二元性質吻合
- 位移 79 的語境含 `DE5EFF.BIN` / `DE51EFF.BIN`,而 [`60`](60-g-closure-2.md)
  已知那是**每個迷宮一個**的事件檔 —— 要選檔就要有迷宮編號
- 位移 31 出現在 `TOWN` 而不是別處,且與 `'Welcome to the'` 同區

⚠ 位移 79 先前([`101`](101-groups-record-status.md) §1)被猜成「迷宮內座標」,
理由是它與世界座標 `ds:3518h`/`ds:351Ah` 在 DS 裡相鄰。
**那個猜測作廢** —— 又一次證明 [`101`](101-groups-record-status.md) §2 那條判準:
**「DS 裡相鄰」是弱訊號**。

## 2. `GROUPS.DAT` 現況

| 位移 | 語意 | | 位移 | 語意 |
|---:|---|---|---:|---|
| 23 | 補給品 | | 41 | 朝向 |
| **25** | **所在地圖層** | | **45** | **光源剩餘回合** |
| 27 | 未解 | | 59 | 未解 |
| 29 | 未解 | | **79** | **當前迷宮編號** |
| **31** | **當前城鎮／設施** | | 81 | 未解 |
| 33 | 未解 | | 83 | 未解(比 `99`、`5`)|
| 35 | 世界座標 x | | 85 | 逃跑旗標([`102`](102-flee-exists-retracting-j.md))|
| 37 | 世界座標 y | | | |

**14 個欄位裡 9 個有語意**(先前 4 個),剩 27、29、33、59、81、83 六個。

## 3. D 的狀態

| 部分 | 覆蓋率 |
|---|---|
| `CHARS.DAT` 整數欄位 | 14 / 15 |
| `CHARS.DAT` 字元欄位 | 全部 |
| **`GROUPS.DAT`** | **9 / 14** |
| 記錄長度、筆數、讀端、寫端 | 全部 |

**D 仍不標 RE-DONE。** 剩下六個欄位的語境字串是空的(`CAMP` 裡的 `ds:353Ch`、
`ds:3538h` 附近抓不到任何文字),所以**這一招對它們無效** ——
要換成逐條讀那幾個變數的使用點。

> **判準:一個方法解掉一批之後,剩下的那些通常正是它解不掉的那種。**
> 「再跑一次同一支腳本」對剩下的六個不會有輸出 ——
> 這不是還沒做,是需要換工具。
