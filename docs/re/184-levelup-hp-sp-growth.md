# 184 — 升級的生命/法力成長算式;手冊 p.48/49 那兩張表在程式裡不存在

日期:2026-08-16
接續:[`183-levelup-attribute-growth.md`](183-levelup-attribute-growth.md)、[`140-manual-cross-check.md`](140-manual-cross-check.md) §6
子系統:**D. 角色/隊伍資料**
輸入:`TOWN.EXE`、`BRUN30.EXE`(SHA-256 見 [`00-inputs.md`](00-inputs.md))

## 結論

| # | 結果 | 信心 |
|---|---|---|
| 1 | 生命成長 = `截整(體能 × max(R1,R2) × K) + 1`,K:戰士 `0.666667`、巫師 `0.434783` | **已確認** |
| 2 | 法力成長(只有巫師)= `截整(智能 × max(R1,R2) × 0.5) + 2` | **已確認** |
| 3 | 四個係數都是 DGROUP 初值:`ds:71C2` / `ds:71BA` / `ds:71BE` / `ds:6D98` + `ds:722E` | **已確認**(我自己用 `dgroup_init.py` 查過)|
| 4 | 亂數因子是**兩次 `RND` 取大**,不是取小 | **已確認** |
| 5 | `ds:6D8C = 255` 是生命與法力**共用**的總量上限,夾的是總量不是成長量 | **已確認**(`0x10306` 寫 `0FFh`,全檔唯一寫入端)|
| 6 | `ds:6D8E = 20` = 等級上限 | **已確認**(`0x1030C` 寫 `14h`)|
| 7 | 升級**不寫**位移 28(當前生命)/ 32(當前法力),也**不扣**經驗 | 證據充分 |
| 8 | **手冊 p.48/49 那兩張 `MAX … GAIN PER LEVEL` 表在程式裡不存在** | 證據充分 |
| 9 | 浮點轉整數是**截尾還是四捨五入** | ⛔ **未解**,見 §5 |

## 1. 兩條分支

`0x110D1` 依職業分岔,兩條的形狀完全相同,只差係數:

```
巫師 0x110D6:                        戰士 0x11117:
  ds:66D0 = 16h ; call loc_10079       同左        ; 讀 CHARS.DAT 位移 22 = 體能
  INT 3F:57                            同左        ; 整數 → 浮點
  mov di, 71B0h / INT 3F:91            同左        ; × 亂數因子(§2)
  mov di, 71BAh / INT 3F:91            mov di, 71C2h / INT 3F:91   ; × 係數 ★唯一的差別
  INT 3F:85                            同左
  mov di, 71BEh / INT 3F:81            同左        ; + 1
  INT 3F:77                            同左        ; → 整數
  mov ds:71B8h, ax                     同左
```

⚠ **IDA 在 `0x11102`–`0x1110F` 與 `0x110ED` 附近切錯指令**
(把 `INT 3F` 的內嵌位元組併進下一條,產生 `cmp word ptr [bx+71BEh], 3FCDh`
這種不存在的指令)。上面的還原是逐位元組重切的結果 ——
**不要拿 IDA 那一段的助憶碼當證據**。

係數的初值(用 [`tools/dgroup_init.py`](../../tools/dgroup_init.py) 查):

```
71C2  ab aa 2a 80  MBF 0.666667   戰士        (≈ 2/3,但**比 2/3 大**)
71BA  d4 9b 5e 7f  MBF 0.434783   巫師        (≈ 1/2.3)
71BE  00 00 00 81  MBF 1          加項
```

法力那條在 `0x11221`–`0x11277`,同樣的形狀,係數 `ds:6D98` = 0.5、加項 `ds:722E` = 2。

## 2. 亂數因子是「兩次取大」

```
011221  INT 3D:34                    ; RND
011224  mov di, 71B0h / INT 3F:7D    ; ds:71B0 ← R1
01122A  INT 3D:34                    ; RND
01122D  mov di, 71B4h / INT 3F:7D    ; ds:71B4 ← R2
011235  mov si, bx ; INT 3F:9F       ; 比較,CF = [si] < [di]
01123A  jb → xchg di, si ; INT 3F:7B ; R1 < R2 時把 R2 抄進 71B0
```

`si` = `71B0`(R1)、`di` = `71B4`(R2),`jb` 成立時複製 —— 所以
**留在 `71B0` 的是大的那個**。期望值 **2/3** 而不是 1/2,平均成長量看得見差別。

## 3. 總量上限

```
010306  mov word ptr ds:6D8Ch, 0FFh   ; 255
01030C  mov word ptr ds:6D8Eh, 14h    ; 20 = 等級上限
...
011157  mov ax, ds:6D8Ch              ; 生命:夾總量
0112C8  cmp ax, ds:6D8Ch              ; 法力:同一個變數
```

⚠ **生命與法力共用同一個上限**,而且夾的是「舊值 + 成長」的**總量**。

## 4. 手冊那兩張表不在程式裡

`INT(屬性 × K) + 加項` 對手冊 **HP 表 36 格全中**。也就是說手冊那張表是這條公式的
**推論**,不是程式裡的查表 —— 全 100 個檔案掃過,沒有對應的資料表。

這推翻的是 `rules.MaxHPGain` / `MaxSPGain` 的**存在理由**,不是它們的值:
HP 兩欄的值就是公式的上界(對的),**SP 那欄與程式對不上**(§5)。
正確的修法是換掉整條算式,不是留著查表再修幾格。

## 5. ⛔ 未解:截尾還是四捨五入

`INT 3F:77`(浮點 → 整數)有兩種行為,由 `ds:1Fh` 分流,而**那個旗標找不到寫入端**
(五種直接寫入形狀全檔 0 命中,推測是指標間接寫)。

差別是**每一級 1 點法力**:

| 智能 20 的巫師 | 單級法力成長上界 |
|---|---|
| 截尾 | **11** |
| 四捨五入 | **12** |

⚠ **生命那條分不出來**:戰士係數 `0.666667` 比 2/3 **大**(相對餘裕 6×10⁻⁷),
而 MBF 的 `1 − R` 只有 2⁻²⁴ ≈ 6×10⁻⁸ —— 所以在體能 3/6/9/12/15/18 這些
「`體能 × K` 剛好整數」的點上,乘積仍然 **大於**那個整數,截尾也拿得到手冊的值。
**只有法力分得出來**,因為它的係數 0.5 是精確的。

引擎目前選**截尾**並在 `train.go` 標成具名假設。

> **裁決方法(第 1 級證據,一次實測就夠)**:在原版把巫師的智能練到 20,
> 反覆升級,量最大法力成長是 **11** 還是 **12**。

⚠ 這一條先前被寫成「證據充分:走截尾」。**那個信心等級不成立** ——
支持它的比對(`INT(屬性 × K) + 加項` 對手冊 SP 表只差 5 格)其實假設了
亂數因子可以取到 1.0,而 `RND` 的值域是 `[0,1)`。把 `R < 1` 算進去之後,
那個比對結果**支持的是四捨五入**,不是截尾。

## 6. 明列剩餘的不確定

| 項目 | 狀態 |
|---|---|
| `ds:1Fh`(截尾/四捨五入的分流旗標)的寫入端 | **未知**,見 §5 |
| `INT 3F:85` 的語意 | **未知**(位置固定在「× 係數」與「+ 加項」之間)|
| 手冊 SP 表從智能 11 起換規則的原因 | **未知** |
| `屬性 × K` 剛好整數時的最後一位邊界 | **未驗**(需精確 MBF 模擬或實測;機率量級 2⁻²⁴)|
