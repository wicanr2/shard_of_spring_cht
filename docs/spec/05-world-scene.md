# 世界地圖場景 — **READY**

M2 的實作規格。資料格式在 [`formats/05`](../formats/05-world-map.md),
版面在 [`04-display-layout.md`](04-display-layout.md),本篇只講**這個場景怎麼運作**。

## 1. 地圖

```
103 × 121 = 12,463 格
索引 = y × 103 + x
每格 2 bytes,little-endian;地形值在**低位元組**(高位元組全 0)
```

⚠ 座標系是 **`y × 寬 + x`**。迷宮是 **`欄 × 81 + 列`**,兩者**不同**
([`00-index.md`](00-index.md) 實作前必讀第 4 條)。這是最容易寫反的一處,
而寫反了地圖仍然畫得出來 —— 只是轉了 90 度,看起來像「地圖本來就長這樣」。

## 2. 地形值總表(實測全 12,463 格)

31 種值。**圖塊來源**依 [`formats/05`](../formats/05-world-map.md) §3:
值 1–9 與 11 走**點陣路徑**;其餘走**向量路徑** = `WRLDITEM.PIC` 的
**第 (值−10) 行,0-based,含空行**([`formats/05`](../formats/05-world-map.md) §3)。
⚠ 空行不可濾掉,經過見 [`re/130`](../re/130-retro-why-the-tile-map-was-missed.md)。

| 值 | 格數 | 佔比 | 語意 | 來源 | 出處 |
|---:|---:|---:|---|---|---|
| **0** | 224 | 1.80% | **地圖邊界**(224/224 全在最外圈)| ❌ 無 | 本篇 §2.1 |
| 1, 2 | 3,419 | 27.4% | 草原(兩種變化)| FASTWRLD | [`re/53`](../re/53-world-tiles-towns-and-draw-renderer.md) |
| 3, 4 | 710 | 5.7% | 森林(疏 / 密)| FASTWRLD | 同上 |
| **5, 6** | 123 | 1.0% | **水**(程式碼把 5/6/11 歸為同一組,[`re/129`](../re/129-world-tile-dispatch.md) §3)| FASTWRLD | ⚠ 推翻了 `re/53` 的「疏林/沼澤」猜測 |
| 7, 8, 9 | 578 | 4.6% | 山(三種密度)| FASTWRLD | 同上 |
| 8 | 178 | — | ⚠ 同時是**另一型地城入口**(座標 35,86)| — | [`re/51`](../re/51-mazedata-and-world-entrances.md) §2 |
| 10 | 30 | 0.24% | 未指認 | WRLDITEM 0 | — |
| **11** | **6,933** | **55.6%** | **海洋** | **點陣**(`ds:0CFA6h`,來源檔未解)| [`re/129`](../re/129-world-tile-dispatch.md) |
| 12, 13 | 9 | 0.07% | 未指認;**會觸發遭遇檢查**(程式碼證據見 [`re/129`](../re/129-world-tile-dispatch.md) §4)| WRLDITEM 2–3 | [`re/60`](../re/60-g-closure-2.md) |
| 15–18 | 372 | 3.0% | 海岸線 | WRLDITEM 5–8 | [`re/53`](../re/53-world-tiles-towns-and-draw-renderer.md) |
| 20, 21 | 5 | 0.04% | 未指認;會觸發遭遇檢查 | WRLDITEM | [`re/60`](../re/60-g-closure-2.md) |
| **24, 25, 27, 28** | **11** | 0.09% | **地城入口** | WRLDITEM | [`re/51`](../re/51-mazedata-and-world-entrances.md) |
| **30, 31, 32** | **13** | 0.10% | **城鎮**(正好 13 個)| WRLDITEM | [`re/53`](../re/53-world-tiles-towns-and-draw-renderer.md) |
| 35–38 | 36 | 0.29% | 未知 | WRLDITEM 25–28 | §2.2 |

### 2.1 值 0 = 地圖邊界

224 格**全部落在最外圈**(x=0 或 x=102 或 y=0 或 y=120),
內陸一格都沒有。**信心等級:已確認**(224/224,不是巧合)。

⚠ 但 224 ≠ 完整外圈的 444 格。實測拆解:

```
上緣整列  y=0        103 格   全部是 0
左緣整欄  x=0        120 格   全部是 0(不含已計入的角)
右緣落單  (102, 120)       1 格
下緣           0 格
                    ─────
                    224 格
```

右緣(x=102)與下緣(y=120)其餘格子**全是海洋(11)**。
所以它不是「畫一圈牆」,比較像**索引從 1 起算留下的第 0 列與第 0 欄**。

> 這個推論**沒有讀取端佐證**,不要據以實作。實作上只要知道
> 「值 0 沒有圖塊、不該出現在玩家可達的區域」就夠了。
> 右緣那一格落單的成因**未解**。

### 2.2 只有值 0 沒有圖塊來源

| 值 | 格數 | |
|---:|---:|---|
| 0 | 224 | 地圖邊界,全在最外圈 |

其餘 12,239 格全部有來源:1–9 與 11 走點陣,10 與 12–38 走
`WRLDITEM.PIC` 的第 (值−10) 行。

⚠ **值 11(海洋)的點陣圖來源檔未解** —— 派工鏈指向 `ds:0CFA6h`,
但那不在 `fastwrld.bin` 的 828 bytes 內([`re/129`](../re/129-world-tile-dispatch.md) §6)。
實作上先畫佔位符。

## 3. 視野

主視野 9×9 格([`04-display-layout.md`](04-display-layout.md) §2:
612 = 9 × 17 × 4,而 153×153 正是原版的圖畫區)。

**隊伍固定在正中央第 (4,4) 格,地圖捲動。**

⚠ 這是**設計決定,不是 RE 結論** —— 還沒讀 `WRLDMOVE` 的繪製迴圈。
另一種可能是視野以區塊為單位跳頁。若之後讀到程式碼證明是跳頁,以那個為準。

視野超出地圖邊界時填**值 0 的佔位符**,不做環繞。

## 4. 繪製

```
for vy in 0..8:
  for vx in 0..8:
    mx = party.x - 4 + vx
    my = party.y - 4 + vy
    tile = 界外 ? 0 : terrain(mx, my)
    畫 tile 的圖 於 (View.X + vx*68, View.Y + vy*68)
```

圖塊來源:

| 值 | 來源 |
|---|---|
| 1–9 | `FASTWRLD.BIN` 第 (值−1) 張(點陣)|
| 11 | 點陣,`ds:0CFA6h`;**來源檔未解** → 暫畫佔位符 |
| 其餘 | `WRLDITEM.PIC` **第 (值−10) 行**(0-based,含空行)的 `DRAW` 向量圖 |
| 0 | **無來源**(地圖邊界)→ 畫佔位符 |

**佔位符必須看得出來是「缺」**,不能用黑色或海洋填 ——
那會讓未解的 36 格偽裝成正常地形,而 §2.2 的未知就此消失在畫面裡。
用洋紅色 `#FF00FF` 的網格塊。

> **判準**:未解的東西在畫面上要**刺眼**。
> 這與 `CLAUDE.md` 的「不要留白讓後人以為漏了」同一條原則,
> 只是搬到了顯示層。

## 5. 可通行性:**已解**(八條規則)

完整規則與位址見 [`re/131`](../re/131-world-passability.md) §2。摘要:

| # | 不可通行的條件 |
|---:|---|
| 1 | `X < 5` 或 `X > 98`(⚠ 兩側各有幾欄玩家永遠到不了;Y 沒有對應檢查)|
| 2 | 目標地形 **10–12**(11 = 海洋;10 與 12 語意未知但同樣阻擋)|
| 3 | 目標 ∈ {20, 21} 且 現在地形 ∉ [1, 4] |
| 4 | 現在 ∈ {20, 21} 且 目標 15–18 |
| 5–8 | **海岸線的方向性阻擋**,見下 |

### 海岸線 15–18 是四個轉角

| 值 | 擋住的方向 | 幾何 |
|---:|---|---|
| 15 | 北、東 | 東北角 |
| 16 | 北、西 | 西北角 |
| 17 | 南、西 | 西南角 |
| 18 | 東、南 | 東南角 |

⚠ 這是**雙向的牆**:北擋 15/16→17/18、南擋 17/18→15/16。
不要寫成「單向通行」。

⚠ **規則 2 不要簡化成「海洋不能走」** —— 範圍是 10–12,
寫成海洋會漏掉值 10(30 格)與 12(8 格)。

## 6. 移動

按鍵([`re/70`](../re/70-key-chains-all-modules.md) §1、§3):

| 鍵 | 方向 | 小鍵盤 |
|---|---|---|
| `1` | 北 (y−1) | ↑ |
| `2` | 東 (x+1) | → |
| `3` | 南 (y+1) | ↓ |
| `4` | 西 (x−1) | ← |

⚠ **朝向不同時只轉身,不位移**(`re/70` §3:`cmp ds:3520h, 1` 在位移之前)。
所以往北走的第一下若原本朝東,只會轉成朝北。**這一條要照做** ——
它會改變玩家的操作手感,也會改變每一步消耗的遊戲時間。

朝向編碼 `1`北 `2`東 `3`南 `4`西,與 `GROUPS.DAT` 位移 41 相同
([`formats/02`](../formats/02-groups-dat.md))。

指令鍵:`C` = 紮營、`S` = 存檔、`P` / `Q` / `J` 語意未解(`re/70` §4)。
**M2 只接方向鍵**,指令鍵留到對應場景存在之後。

## 7. 時鐘與遭遇倒數

每次**實際位移**(不含純轉身)推進時鐘一格
([`formats/02`](../formats/02-groups-dat.md) 的四級進位),並 `dec` 遭遇倒數
(`GROUPS.DAT` 位移 25)。倒數歸零時掃 3×3 鄰域做遭遇檢查
([`re/122`](../re/122-d-closure.md))。

⚠ **M2 不實作遭遇** —— 戰鬥是 M4。M2 只把時鐘與倒數推進做對,
並在訊息列顯示目前的值,讓 M4 接手時有東西可對。

## 8. M2 的範圍

| 做 | 不做 |
|---|---|
| 讀 `worldmap.json`,9×9 視野繪製 | 遭遇與戰鬥(M4)|
| 方向鍵移動 + 先轉身規則 + **可通行性八條規則** | — |
| 時鐘四級進位、遭遇倒數遞減 | 進城鎮 / 進地城(M5 之後)|
| 未知地形畫成刺眼的佔位符 | 光源與能見度(需要 `CHARS`/`GROUPS`,M3)|

**驗收**:103×121 地圖畫得出來、座標與原版一致、
`WRLDITEM.PIC` 的 `DRAW` 圖塊渲染得出來。

## 9. 尚未解開

| 項目 | 狀態 |
|---|---|
| 地形值 35–38 | 未知(§2.2)|
| 值 0 為何只有上緣與左緣 | 假設是 1-based 索引的殘留,無讀取端佐證 |
| 視野是捲動還是跳頁 | 設計上採捲動,未讀繪製迴圈(§3)|
| 地形值 5/6 是疏林還是沼澤 | 假設([`re/53`](../re/53-world-tiles-towns-and-draw-renderer.md))|
